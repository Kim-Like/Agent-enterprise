# IAn Agency Contexts

This folder holds mapped source context that remains operationally relevant but is not presented as a top-level client program.

Current contents:

- `samlino/`

These assets are kept explicit so AI-Enterprise retains portfolio memory without promoting every brownfield module to a first-class sellable program.
