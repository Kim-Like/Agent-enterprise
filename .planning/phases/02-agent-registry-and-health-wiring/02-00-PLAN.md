---
phase: 02
slug: agent-registry-and-health-wiring
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - "/Users/IAn/Agent/Agent Enterprise/agents/registry.json"
  - "/Users/IAn/Agent/Agent Enterprise/server/src/lib/agent-registry.js"
  - "/Users/IAn/Agent/Agent Enterprise/server/src/db/init.js"
  - "/Users/IAn/Agent/Agent Enterprise/server/src/app.js"
  - "/Users/IAn/Agent/Agent Enterprise/tests/server/agent-registry.test.js"
autonomous: true
requirements:
  - REG-01
  - REG-03
user_setup: []
must_haves:
  truths:
    - "Runtime-capable agent records must still be explicit and opt-in, not inferred from folder presence."
    - "Phase 2 has to normalize the existing Phase 1 registry shape before health or adapter logic depends on it."
    - "Persisted agent state must stay inside the existing SQLite file instead of creating a second state store."
  artifacts:
    - "/Users/IAn/Agent/Agent Enterprise/agents/registry.json"
    - "/Users/IAn/Agent/Agent Enterprise/server/src/lib/agent-registry.js"
    - "/Users/IAn/Agent/Agent Enterprise/tests/server/agent-registry.test.js"
  key_links:
    - "This wave upgrades the registry from inventory-only metadata into a safe activation-ready model."
---

<objective>
Upgrade the agent registry, normalization layer, and persisted agent-state foundation for runtime-capable records.

Purpose: turn the static Phase 1 inventory into a validated Phase 2 registry model without enabling execution by default.
Output: registry v2 shape, normalization/service layer, and Wave 0 registry tests.
</objective>

<execution_context>
@/Users/IAn/.codex/get-shit-done/workflows/execute-plan.md
@/Users/IAn/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-agent-registry-and-health-wiring/02-RESEARCH.md
@.planning/phases/02-agent-registry-and-health-wiring/02-VALIDATION.md
@agents/registry.json
@server/src/app.js
@server/src/db/init.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade the agent registry shape to a Phase 2 activation model</name>
  <files>/Users/IAn/Agent/Agent Enterprise/agents/registry.json</files>
  <action>Expand the registry records so agents can express enablement state, adapter identity, typed dependency references, health probes, and explicit hold reasons while keeping risky or legacy agents disabled by policy.</action>
  <verify>cd '/Users/IAn/Agent/Agent Enterprise' && npm test -- --test-name-pattern="agent|registry"</verify>
  <done>The registry can represent which agents are merely visible, which are blocked, and which are safe to enable.</done>
</task>

<task type="auto">
  <name>Task 2: Add the registry normalization layer and persisted agent-state helpers</name>
  <files>/Users/IAn/Agent/Agent Enterprise/server/src/lib/agent-registry.js, /Users/IAn/Agent/Agent Enterprise/server/src/db/init.js, /Users/IAn/Agent/Agent Enterprise/server/src/app.js</files>
  <action>Create the server-side loader that validates and normalizes Phase 2 registry records, extend SQLite helpers for per-agent state and last-health metadata, and attach the normalized registry service to the existing app boot seam.</action>
  <verify>cd '/Users/IAn/Agent/Agent Enterprise' && npm test -- --test-name-pattern="agent|registry|state"</verify>
  <done>The server can boot with normalized agent data and persisted agent state without adding a new runtime process.</done>
</task>

<task type="auto">
  <name>Task 3: Add Wave 0 registry tests for the Phase 2 contract</name>
  <files>/Users/IAn/Agent/Agent Enterprise/tests/server/agent-registry.test.js</files>
  <action>Write tests that lock the new registry schema, normalization behavior, typed dependencies, and persisted agent-state helpers so later waves cannot regress into note-driven enablement.</action>
  <verify>cd '/Users/IAn/Agent/Agent Enterprise' && npm test -- --test-name-pattern="agent|registry"</verify>
  <done>Phase 2 has a real validation floor before health or enablement work starts.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd '/Users/IAn/Agent/Agent Enterprise' && npm test -- --test-name-pattern="agent|registry|state"`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Agent records can express explicit enablement and adapter policies
- The server can normalize and persist per-agent state without a second state store

</success_criteria>

<output>
After completion, create `/Users/IAn/Agent/Agent Enterprise/.planning/phases/02-agent-registry-and-health-wiring/02-00-SUMMARY.md`
</output>
