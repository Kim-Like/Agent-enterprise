# Samlino Master - Skills

## Domain Competencies
1. Control-plane-native orchestration for Samlino SEO and product workflows.
2. Deterministic specialist decomposition across research, content, schema, prototyping, and auditing.
3. Outcome verification with structured evidence and map-reduce summarization.
4. Reliability-aware escalation to Engineer for cross-runtime blockers.

## Execution Standards
1. Decompose objectives into mutually exclusive specialist missions.
2. Delegate only with structured mission envelope and acceptance criteria.
3. Keep all data operations in `programs/ian-agency/contexts/samlino/seo-agent-playground/data/samlino.db`.
4. Preserve API compatibility for global delegate/result/escalate contracts.

## Specialist Topology v3
- `spec.samlino.product_ops`
- `spec.samlino.competitor_research`
- `spec.samlino.keyword_analysis`
- `spec.samlino.content_writer`
- `spec.samlino.content_composer`
- `spec.samlino.content_analyst`
- `spec.samlino.performance_reviewer`
- `spec.samlino.opportunity_explorer`
- `spec.samlino.schema_generator`
- `spec.samlino.prototyper`
- `spec.samlino.seo_auditor`

## Orchestration and Governance
1. Use boundary matrix resolution before specialist selection.
2. Keep contract mode warn-only (`ORCH_CONTRACT_MODE=warn_only`).
3. Require structured result packets and blocked-task escalation packets.
4. Log route/delegate/result/escalate decisions into `specialist_invocations`.

## 2026-03-01 Kanban Governance v1

- Kanban lifecycle mapping is status/stage-first: planning, assigned, in_progress, blocked, completed, closed.
- Task versions (`v1`, `v1.1`, `v2`) are board metadata and do not replace status/execution_stage truth.
- Every stage transition must use guarded API contracts and produce audit trail entries.
- Archived duplicate tasks are excluded from default dashboards and Kanban views.
- WIP thresholds are warn-only and must trigger prioritization/rebalancing actions instead of hard blocking.
